import { useEffect, useState } from 'react';import { Card, Table, TableBody, TableCell, TableHead, TableRow, Typography } from '@mui/material';import { listenClientes } from '../../services/clientes/clientesService.js';
export default function Clientes(){const [clientes,setClientes]=useState([]);useEffect(()=>listenClientes(setClientes),[]);return <><Typography variant="h5" mb={2}>Clientes</Typography><Card><Table><TableHead><TableRow><TableCell>Nombre</TableCell><TableCell>Teléfono</TableCell><TableCell>Notas</TableCell></TableRow></TableHead><TableBody>{clientes.map(c=><TableRow key={c.id}><TableCell>{c.nombre}</TableCell><TableCell>{c.telefono}</TableCell><TableCell>{c.notas}</TableCell></TableRow>)}</TableBody></Table></Card></>}
